/**
 * URL publique du backend Railway si aucune variable d’environnement n’est définie.
 * Doit rester identique au fallback dans `next.config.js` (injecté au build).
 */
export const DEFAULT_PUBLIC_BACKEND_URL =
  "https://attentiqbackend-production.up.railway.app";
